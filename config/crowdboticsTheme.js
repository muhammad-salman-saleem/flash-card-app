import { dark, light, mapping } from "@eva-design/eva";
import { default as theme } from './theme.json'

export const crowdboticsTheme = {
    ...light,
    ...theme
    //todo add custom colors here
};

export const crowdboticsMapping = {
    ...mapping,
    // ...customMapping
}
